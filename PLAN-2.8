2.8:
 - gtk/gnome traybar item
 - rewrite stats stuff to use a SQL backend (and have plugin support 
	so we can have SQLite, MySQL and PostGreSQL backends)
 - <network><listen/></network> -> <listen/>
 - authentication subsystem: 
  - pam backend
  - plain text backend (current)
 - encryption techniques (GIOChannel modifiers)
  - nss
  - ssl
  - gnutls
 - Net::IRC-like interface
 - dircproxy-config-to-ctrlproxyrc script
 - more tests in the testsuite
