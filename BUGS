- When trying to get nick X that is already in use the user gets 
  the nick X_. When the channel that X is on is joined, X can not 
  be seen. After a rejoin, X can be seen.
- LINE_IS_PRIVATE doesn't appear to work
