- interpret remaining sections in ~/.ctrlproxy/config as network configurations
- allow multiple connections to one network
- allow setting network-specific configuration using <networkname>/<variable>
